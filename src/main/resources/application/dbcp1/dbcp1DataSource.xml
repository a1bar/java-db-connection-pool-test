<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="
        http://www.springframework.org/schema/beans
            http://www.springframework.org/schema/beans/spring-beans-3.0.xsd">

	<!--<jms:config javaOptions="-server -Xmx64m" jmxPort="7654" />TODO-->

    <bean id="dbcpDataSource" class="org.apache.commons.dbcp.BasicDataSource"
		destroy-method="close" lazy-init="false">
		<property name="driverClassName" value="oracle.jdbc.OracleDriver" />
		<property name="url" value="${datasource.test.common > urls}" />
		<property name="username" value="${datasource.test.common > username}" />
		<property name="password" value="${datasource.test.common > password}" />
		<property name="initialSize" value="${datasource.test.common > maxPoolSize}" />
		<property name="maxActive" value="${datasource.test.common > maxPoolSize}"/>
		<property name="maxWait" value="${datasource.test.common > getConnectionTimeout}"/>
	</bean>

</beans>
